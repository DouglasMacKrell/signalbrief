services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: signalbrief
      POSTGRES_PASSWORD: signalbrief
      POSTGRES_DB: signalbrief
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
